from solution.calculate import find_triangular_number_divisible_by_more_than_n_numbers

solution = find_triangular_number_divisible_by_more_than_n_numbers(500)

print('The solution for Problem 12 is {}'.format(solution))
