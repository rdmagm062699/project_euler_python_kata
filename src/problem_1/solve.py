from solution.calculate import get_sum_of_numbers_divisible_by_three_and_five

value = 1000
solution = get_sum_of_numbers_divisible_by_three_and_five(value)

print('The solution for Problem 1 is {}'.format(solution))
